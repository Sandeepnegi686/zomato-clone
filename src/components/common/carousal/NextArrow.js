function NextArrow({}) {
  // const { className, style, onClick } = propes;
  return (
    <div
      // className={className}
      style={{
        background: "white",
        borderRadius: "50%",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        padding: "4px",
      }}
      // onClick={onClick}
    ></div>
  );
}

export default NextArrow;
